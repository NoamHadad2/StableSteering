{
  "name": "stablesteering-playwright",
  "private": true,
  "version": "0.1.0",
  "description": "Playwright browser tests for StableSteering",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:chrome": "playwright test --project=chrome",
    "test:e2e:debug": "powershell -ExecutionPolicy Bypass -File scripts/run_e2e_debug.ps1"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  }
}
